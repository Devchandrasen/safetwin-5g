# R5 file prerequisites verified; native invocation remains disabled

Date: 2026-09-06 UTC. Evidence label: **fixture**. Branch:
`codex/r5-file-admission`. The parent recovery-validation roadmap item remains
the only in-progress item. No measured recovery or TNSM claim is promoted.

## Completed software component

The [file-admission contract](RECONNECT_R5_ADMISSION.md) now has executable
actual Git/filesystem checks, not injected prerequisite booleans. Its exact
approval-byte, committed-source, staged-source, prior-attempt and persistent
reservation checks pass their software gate. An independent file auditor
passes with candidate acceptance disabled and rejects altered summary flags.

The new manifest is `config/experiments/reconnect-r5-admission-lock.json`:
SHA256 `ba6f6db34139d8aa6916fef963aadb4e563ba5ecec464f2ad4214c82490ceb2f`.
It freezes 153 pre-existing source/dependency files plus five new component
files. Checks include that manifest itself: **159 committed/working files**.
All ten historical R3/R4/R5 locks and their frozen sources remain unchanged.

Final capture:
[`20260906T121622125406Z-reconnect-r5-admission`](../evidence/verification/20260906T121622125406Z-reconnect-r5-admission/report.json).
**894 tests and 200 subtests passed in 348.92 seconds**, including 68 new
file-admission tests. Full regression has no deselection; source hashes are
unchanged; the new fixture archive's 23,886 members match their local bytes.
The full-suite runtime is software verification time, not network latency.

Separately rechecked during this turn: recursive committed R5 execution lock,
the statistical analysis lock, and an independent archive read of all 153
authoritative frozen files with **zero byte mismatches**. Actual R5 approval
and runtime-receipt paths remain absent. No actual reservation, packet probe,
fault, image change, service restart, power request or native clock recapture
was executed. The positive cases use invented approvals in temporary Git
repositories inside verification artifacts, never the authoritative checkout.

## Preserved development outcomes

| Capture under `evidence/verification/` | Result |
|---|---|
| `20260906T120610518298Z-reconnect-r5-admission` | 1 pass, 60 setup errors; long fixture paths |
| `20260906T120812515771Z-reconnect-r5-admission` | 51 pass, 10 fail; archive line-ending conversion |
| `20260906T121227474805Z-reconnect-r5-admission` | 67 pass; focused corrections verified |
| `20260906T121622125406Z-reconnect-r5-admission` | 894 pass, 200 subtests pass; final full regression |

Every capture retains its original pre-test `sources.zip`, source hashes,
raw `pytest.log`, report and complete file-hash manifest. These are committed.
Expanded temporary fixtures and the **lossless `admission-fixtures.zip` files
remain local and are deliberately not included in Git**. They were neither
deleted nor rewritten. This is not a claim that a Git-only clone includes
every generated temporary Git object. The verifier can regenerate fixtures;
the original local archives preserve failures and their exact input bytes.
Unchanged large old-protocol test outputs generated by the full suite are
also retained locally and covered by its manifest.

## Debug report

Status: **DONE** for the two reproduced software defects. The `investigate`
workflow required retaining the original failures, confirming each cause,
applying narrow changes, and then running the entire test suite. Skill
telemetry, global settings and memory writes were not performed; the work
stayed inside the authorized repository.

1. Fixture setup tried to write a **261-character** filename while its parent
   existed. The error repeated across all 60 file cases. Short, stable hashed
   case directories remove excess pytest-name nesting; `case.json` retains
   the full test identity. No registry, global path policy or production
   admission rule was changed. This diagnosis matches Microsoft's documented
   [Windows path-length limitation](https://learn.microsoft.com/en-us/windows/win32/fileio/maximum-file-path-limitation).
2. The candidate matched raw Git blobs, but Git archive inherited
   `core.autocrlf=true`. For example, `config/actions.json` expanded from 1,120
   bytes with zero CRLF pairs to 1,167 bytes with 47 CRLF pairs. The independent
   auditor now supplies a command-local `core.autocrlf=false`; exact hash
   comparison stays unchanged and evidence bytes are not normalized. The
   added regression explicitly sets true in its temporary repository and
   confirms that audit passes without changing that setting. The same raw
   archive check also passed on the authoritative historical files.

Further tested boundaries include wrong/revoked/expired approval, bool versus
integer scope substitution, duplicate/nonfinite JSON, working/index/HEAD
drift, weakened manifest, redirected Git environment, hard-linked approval,
owned-path replacement, persistent rejection after approval restoration,
fsync failure and two concurrent actual filesystem contenders. Only one
contender reserves; no cleanup deletes the persistent receipt.

## Remaining gate, not implied completion

This component does not dispatch commands or authenticate a human identity.
Do **not** reserve the authoritative R5 receipt at this file-only stage.
The frozen fixture runner still refuses native mode. The next software step
is a separately versioned native invocation and matching independent
whole-protocol audit integrating these file checks into the shared clock and
host ownership lifecycle, including independent rollback after revocation.
The two receipt implementations must not both acquire the same path.

Only after that integration passes and is committed may a later execution
decision and explicit human approval assess one bounded sandbox diagnostic.
Live actuation stays forbidden. No new campaign, dataset salvage, model
promotion, hardware/operator claim or positive paper follows from this pass.
